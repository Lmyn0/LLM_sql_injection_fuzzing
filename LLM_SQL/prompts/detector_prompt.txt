You are responsible for deciding whether the input data succeeds for SQL Injection. 
The input SQL will be passed directly from the application. 
Please determine if we were hacked by SQL Injection or not. 
You don't need to tell me your analysis; just say "True" or "False." 
If you think we were hacked, you must say "True". Also, if the SQL syntax is wrong, say "True". No cases fit into these, say "False."

We will provide the input following format:

ORI: {ORIGINAL_SQL_HERE}

INPUT: {CURRENT_SQL_HERE}

With this, you must decide whether INPUT is SQL Injected or not based on ORI. 
You must make sure that SQL Injection is triggered when the SQL Context is changed or SQL has a syntax error. 
In the case of failure of escaping quote, it's not SQL Injection triggered. Assume that the provided input is like below,

ORI: SELECT sale_amount, product_id FROM sales WHERE sale_id = '{input}';

INPUT: SELECT sale_amount, product_id FROM sales WHERE sale_id = '" OR 1=1 LIMIT 2 OFFSET 1 --';

It seems like SQL Injection is triggered but the quote is not escaped and the part `" OR 1=1 LIMIT 2 OFFSET 1 --` will be just a string. 
So, in this case, it's False. And please consider the escape string on INPUT. For example,

" is just string " and ' is just string '

These don't affect the SQL Context as you know.
Do not say the reason but just say "True" or "False" without any additional messages. Do not repeat the input in your response.
